# De-NDIS execution progress (ralph-loop controller state)

Plan: `docs/superpowers/plans/2026-06-23-generic-invoicing-de-ndis.md`
Branch: `feat/generic-invoicing-de-ndis`
Mode: subagent-driven-development. One implementer at a time, then spec review, then code-quality review, fix-loop until both pass, then next.

Completion promise: `ALL_PHASES_GREEN` — emit only when Phase 8 final `-race` gate + frontend gate are green.

## Phase status
- [x] Phase 0 — baseline green (Go + frontend), branch created
- [x] Phase 1 — gst_free→taxable (DONE: commits b4ce89d, bedf39a + nit-fix; spec ✅, code-quality ✅ approve)
- [ ] Phase 2 — participant→client (+type, reference)
- [x] Phase 3 — plan_manager→payer (DONE: commit 571bb7e; gate green; review pending)
  NOTE: a mid-task hard-reset (concurrent worktree reviewer) wiped commits eae7087 (service.go comment fix) + b396d65 (tracker). Comment fix re-applied. LESSON: never run a reviewer while an implementer is active — strictly serialize.
- [ ] Phase 4 — shift→session
- [ ] Phase 5 — catalog→pricelist/items (+unit_price, category)
- [ ] Phase 6 — data-presence validation + client.type gating
- [ ] Phase 7 — generic upload-and-map import
- [ ] Phase 8 — docs + final -race gate

## Controller loop each iteration
1. `git log --oneline` + this file → determine current phase.
2. If an implementer is mid-flight, WAIT for its completion notification (no parallel implementers).
3. On implementer DONE → dispatch spec-compliance reviewer; on ✅ → code-quality reviewer; fix-loop via same implementer until both ✅.
4. Tick the phase here + in the plan, commit, dispatch next phase implementer.
5. After Phase 8: run full `-race` + frontend gate. If green → emit the promise.
