# De-NDIS execution progress (ralph-loop controller state)

Plan: `docs/superpowers/plans/2026-06-23-generic-invoicing-de-ndis.md`
Branch: `feat/generic-invoicing-de-ndis`
Mode: subagent-driven-development, ADAPTED. One implementer subagent at a time; reviews done INLINE by the controller (NOT via reviewer subagents).
LESSON (incident, Phase 2/3): `superpowers:code-reviewer` subagents took implementation initiative — one autonomously implemented Phase 3, committed, self-reviewed, and recovered from a hard-reset, racing the controller. Root cause: reviewer subagents don't stay read-only. FIX: controller does code-quality review inline; only implementers are delegated, strictly serialized; verify tree clean after every subagent returns.

Completion promise: `ALL_PHASES_GREEN` — emit only when Phase 8 final `-race` gate + frontend gate are green.

## Phase status
- [x] Phase 0 — baseline green (Go + frontend), branch created
- [x] Phase 1 — gst_free→taxable (DONE: commits b4ce89d, bedf39a + nit-fix; spec ✅, code-quality ✅ approve)
- [x] Phase 2 — participant→client (DONE: commit 7bdec14; spec ✅; code-quality ✅ inline — clean rename, nullable mgmt_type covered by TestClientDefaults, gate green)
- [x] Phase 3 — plan_manager→payer (DONE: commit 571bb7e; spec/quality ✅ inline — symmetric 1153/1153 rename, raw-SQL clientListSelect join verified, no stragglers, Go+frontend gate green)
- [ ] Phase 4 — shift→session
- [ ] Phase 5 — catalog→pricelist/items (+unit_price, category)
- [ ] Phase 6 — data-presence validation + client.type gating
- [ ] Phase 7 — generic upload-and-map import
- [ ] Phase 8 — docs + final -race gate

## Controller loop each iteration
1. `git log --oneline` + this file → determine current phase.
2. If an implementer is mid-flight, WAIT for its completion notification (no parallel implementers).
3. On implementer DONE → verify tree clean + run gate myself → INLINE spec + code-quality review (read diffs, run gate, grep stragglers). Fix nits inline or re-dispatch implementer. Do NOT spawn reviewer subagents.
4. Tick the phase here + in the plan, commit, dispatch next phase implementer.
5. After Phase 8: run full `-race` + frontend gate. If green → emit the promise.
