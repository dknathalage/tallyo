# De-NDIS execution progress (controller state)

Plan: `docs/superpowers/plans/2026-06-23-generic-invoicing-de-ndis.md`
Branch: `feat/generic-invoicing-de-ndis`
Mode: subagent-driven-development, ADAPTED. One implementer subagent at a time, strictly serialized; reviews done INLINE by the controller (NOT via reviewer subagents). Ralph loop CANCELLED (it caused uncoordinated concurrent progress).
LESSON (incident, Phase 2/3): reviewer subagents took implementation initiative and raced the controller; ralph re-feed + context compaction compounded it. FIX: controller reviews inline; only implementers delegated; verify tree clean + gate green after every subagent returns; never run anything while an implementer is active.

## KEY CONSTRAINT — session naming collision (Phase 4)
Control DB has `sessions` table (scs auth store, internal/auth/session.go) → gen.Session.
sqlc merges control+tenant into ONE catalog, so the tenant shift table CANNOT be named `sessions`
(duplicate-table error; scs requires its table be `sessions`, can't move it).
DECISION: tenant table = `work_sessions` (gen model `WorkSession`, mapped to domain `Session`);
slice/package/API/UI stay `session` / `/sessions` / "Sessions". DO NOT touch internal/auth or control migration.

## Phase status
- [x] Phase 0 — baseline green (Go + frontend), branch created
- [x] Phase 1 — gst_free→taxable (DONE: commits b4ce89d, bedf39a + nit-fix; spec ✅, code-quality ✅ approve)
- [x] Phase 2 — participant→client (DONE: commit 7bdec14; spec ✅; code-quality ✅ inline — clean rename, nullable mgmt_type covered by TestClientDefaults, gate green)
- [x] Phase 3 — plan_manager→payer (DONE: commit 571bb7e; spec/quality ✅ inline — symmetric 1153/1153 rename, raw-SQL clientListSelect join verified, no stragglers, Go+frontend gate green)
- [x] Phase 4 — shift→session (DONE: committed 6d1e87a/f49023b; verified green after recovery. tenant table work_sessions→gen.WorkSession→domain Session; control `sessions`+scs UNTOUCHED; lifecycle scheduled→recorded→drafted→sent→paid + ClearForInvoice KEPT. gen drift (AuthSession→Session) fixed in a03dec1. Phase 8 polish: agent import/extract smart keeps ImportShifts/ExtractShifts/ShiftDraft vocab — refers to parsed work-period, optional rename later.)
  RACE RESOLVED: a 2nd claude session (bcf17d41, PID 52693) ran the same plan concurrently + left a lingering Phase 5 subagent; user killed the session, subagent stopped. Base reset to f49023b, gen resynced (a03dec1). Single-owner now.
- [x] Phase 5 — catalog→pricelist/items (DONE: commit 37a62a2; inline review ✅ — slice/tables/queries/gen renamed, items.unit_price + category added, 3 NDIS cols collapsed, line_items item_id/price_list_version_id, validation logic UNCHANGED, Go+frontend gate green, gen in sync. Minor: test-helper names seedNoteCatalogVersion/TestValidateSupportItem* kept — Phase 8 polish.)
  RALPH PERMANENTLY REMOVED: .claude/ralph-loop.local.md was tracked in git (committed at iter 12) so every reset resurrected it + re-armed the stop-hook. Untracked+gitignored in 6c41c65. Loop is dead. Base now 37a62a2.
- [x] Phase 6 — data-presence validation + client.type gating (DONE: commit 6c52495; inline review ✅ — tenantZone returns "" when no zone → zone-cap/fill block SKIPPED for generic tenants; NDIS zone path + fill mode PRESERVED (TestValidateOverCapRejected/FillingOverwrites pass); client.type gating (ndis requires plan fields, invalid type→clean 400 via errInvalidType); generic happy-path locked; Go+frontend gate green. tax/snapshot/custom logic untouched.)
- [x] Phase 7 — generic upload-and-map import (DONE: commit 8b02c81; inline review ✅ — importer.ApplyMapping (6 tests), pricelist Inspect/ImportMapped (persist-nothing + atomic + rollback tested), import/inspect+import/commit endpoints owner/admin-gated, frontend wizard, NDIS ParseXLSX+constants DELETED, no deps added, Go+frontend gate green incl -race on affected pkgs.)
- [x] Phase 8 — docs + final -race gate (DONE: docs commit 2d994d9 — CLAUDE.md + data-model.md + DB-per-tenant ERD updated, docs-only. FINAL GATE GREEN: gofmt/vet clean, CGO_ENABLED=0 build OK, `go test ./... -race` = 0 failures / 26 ok, frontend check 0/0 + build ✔. Straggler sweep: no live old-name identifiers/columns/json; remaining hits are test-helper param names (gstFree→inverts to taxable), test fn names, and sample SSE strings in generic infra tests — cosmetic, acceptable. 54 `ndis` refs in non-test go are the intended opt-in concept.)

## ✅ PLAN COMPLETE — all 8 phases done, full -race gate green. Generic invoicing core with NDIS as an optional, data-driven capability.

## Controller loop each iteration
1. `git log --oneline` + this file → determine current phase.
2. If an implementer is mid-flight, WAIT for its completion notification (no parallel implementers).
3. On implementer DONE → verify tree clean + run gate myself → INLINE spec + code-quality review (read diffs, run gate, grep stragglers). Fix nits inline or re-dispatch implementer. Do NOT spawn reviewer subagents.
4. Tick the phase here + in the plan, commit, dispatch next phase implementer.
5. After Phase 8: run full `-race` + frontend gate. If green → emit the promise.
