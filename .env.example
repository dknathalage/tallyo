# Server configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Database configuration
# Path where SQLite database will be stored
DB_PATH=~/.invoices/invoices.db
# For Docker, use: DB_PATH=/data/invoices.db
