# routes/invoices/

Invoice pages.

- `+page.svelte` — List all invoices with search, filter, bulk actions
- `new/+page.svelte` — Create a new invoice
- `[id]/+page.svelte` — View invoice detail with PDF preview
- `[id]/edit/+page.svelte` — Edit an existing invoice
