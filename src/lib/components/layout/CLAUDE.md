# components/layout/

App-level layout components.

- `AppShell.svelte` — Main app wrapper with sidebar/content structure
- `FileGate.svelte` — Gate that ensures database is loaded before rendering children
- `Navbar.svelte` — Top navigation bar
