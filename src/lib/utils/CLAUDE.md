# src/lib/utils/

Utility functions with co-located tests.

- `format.ts` — Date and currency formatting helpers
- `invoice-number.ts` — Invoice number generation logic
- `pdf.ts` — PDF invoice generation using jsPDF + autotable
